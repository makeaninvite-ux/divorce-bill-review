Upload the contents of this folder to a GitHub repo, then import that repo into Vercel.

After Vercel imports it:
1. Add an environment variable named ANTHROPIC_API_KEY
2. Redeploy

This starter uses the Next.js App Router with plain JavaScript files to avoid TypeScript setup problems.
